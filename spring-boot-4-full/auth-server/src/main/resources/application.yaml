server:
  port: 8080

spring:
  application:
    name: auth-server

  datasource:
    url: jdbc:mysql://localhost:3307/authdb?allowPublicKeyRetrieval=true&useSSL=false
    username: authuser
    password: authpw
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false